## 1. Capture tooling

- [x] 1.1 Add an exported capture helper in a new `login/chrome/export_test.go` (internal `package chrome`, `_test.go` file only - not `options.go`, not part of the public API) that sets an unexported `screenDumpDir` field through `chromeLogin`/`Screens` via the same `login.Option`/`Apply` mechanism other options use; verify with a unit test that the field is set and that an empty dir is rejected the same way other options reject invalid input, and separately confirm the symbol is absent from a normal `go build`/`go doc` of the module (only visible under `go test`).
- [x] 1.2 In `login/chrome/screens.go`'s `run()`, when `screenDumpDir` is set, enable CDP's `Network` domain and record every response for the current navigation - the document and every asset it loads (CSS/JS/images) - to `<dir>/<hostname>/<path>`, mirroring each response's real hostname and URL path; verify with a unit test (using the existing `resolver`/`screenResolver` test seam) that dump files are written with the expected hostname-based paths and content when the dir is set, and that nothing is written when it is unset.
- [ ] 1.3 (Attempted, not completed - no committed/curated fixture set exists yet.) Manually run the existing live "Should work" spec in `chrome_test.go` once with the capture helper pointed at a scratch directory against a real account, to produce one real network-response capture per screen (privacy, credentials, otp, trusted-device, final) organized by hostname. Confirm with the user before running (live account, same rate-limit sensitivity as prior live runs). While reviewing the capture, confirm or revisit design.md Decision 4's assumption that the credentials/OTP/trusted-device steps may share one real URL path.

  Progress this session: wired `WithScreenDumpDir` into `chrome_test.go` behind an opt-in `CHROME_SCREEN_DUMP_DIR` env var and ran the live spec several times. This surfaced and fixed two real capture bugs (committed): (a) `network.GetResponseBody(id).Do(ctx)` must go through `chromedp.Run`, not be called directly, or every capture fails with "invalid context"; (b) capture must start in `chromeLogin.login()` before `firstScreen` navigates, not inside `Screens.Resolve()`, or the very first page load is missed. Also confirmed and fixed, per user's suggestion, that Decision 4's shared-URL assumption is real (Keycloak serves credentials/OTP/trusted-device from the exact same path) by adding a collision-suffix scheme (`authenticate`, `authenticate-2`, `authenticate-3`) so same-path captures no longer silently overwrite each other.

  A run with all fixes in place did capture 191 real files across 15 hostnames correctly, including distinct OTP and trusted-device pages - but that output was deleted by a subsequent `rm -rf` before a final run failed with `context canceled` after a ~3 minute stall on the credentials screen, matching the same external SSO-rate-limiting symptom seen on PR #35's task 3.1. No distinctly-captured username/password (credentials) page was ever observed in any run; it's unconfirmed whether this is capture-timing-related or because this account is already "remembered" server-side (skipping straight to OTP). Stopped here at user's request to avoid further live-account load. Re-run once the rate limit has cleared to get and keep a full, curated capture.

## 2. Fixtures

- [ ] 2.1 Commit the captured tree under `login/chrome/testdata/<hostname>/...` (one directory per real hostname involved in the flow, e.g. the SSO hostname and the final `/home` hostname), as raw captures (no manual edits) except the final screen's page; verify each expected page is present and contains the selector(s) its screen matches on (`#footer_tc_privacy_button_3`, `form[name=login-form]`+`#username`+`#password`+`#submit-button`, `#otpCode`, `#save-trusted-device-form`+`#save-trusted-device-later`), and that captured assets (CSS/JS/images) those pages reference are present at their captured relative paths.
- [ ] 2.2 Add the hand-authored session-seeding `<script>` block to the final screen's captured page (clearly delimited with a comment marking it as test-only, not part of the capture) that sets `sessionStorage.access_token`/`app_expires_at` to fixed test values; verify by loading the file directly in a headless chromedp instance in a throwaway test and asserting both `sessionStorage` keys read back the expected values.

## 3. Mock server

- [ ] 3.1 Add `login/chrome/mock_server_test.go` with an `httptest.NewServer` that dispatches each incoming request by its `Host` header + path to the matching file under that hostname's captured directory tree, and refuses (non-200, no fallback) any `Host` not in the captured set; verify with a unit test that requests for each captured hostname+path return the expected fixture content, and a request with an unrecognized `Host` header is refused.
- [ ] 3.2 Launch the browser under test with Chrome's `--host-resolver-rules` mapping each of the flow's real hostnames to the mock server's `127.0.0.1:<port>`, and configure `chromedp` to block requests to any hostname not in that same allow-list; verify with a unit test/spec that a request to an unmapped hostname is blocked rather than reaching the real network.
- [ ] 3.3 Make the mock server set the token-exchange test cookie (matching what the final page's script seeds) when its `/home`-equivalent path is requested; verify with a unit test that request returns a `Set-Cookie` header the test expects.

## 4. Specs against the mock server

- [ ] 4.1 Add `login/chrome/mock_test.go` (package `chrome_test`) that starts the mock server, launches Chrome with the `--host-resolver-rules` mapping from task 3.2, and calls `chrome.New(chrome.WithURL(<real entry hostname's captured URL>), chrome.WithRefreshFrequency(...), chrome.WithBinary(...), ...)` with a well-formed dummy OTP secret - `WithURL` stays the real hostname (so `firstScreen.Do` navigates exactly as it would live); Chrome's resolver override, not the option, is what makes that resolve locally. Assert `Login` succeeds with a valid token and non-empty cookies, with no `DIGIPOSTE_*` env vars required; verify by running `go test ./login/chrome/...` locally with those env vars unset and confirming the new spec runs (not skipped) and passes.
- [ ] 4.2 Add a second spec asserting each screen's own selector-driven interaction is actually exercised (e.g. via the same log-line assertions `chrome_test.go` already uses, such as `"[credentials screen] Resolving screen"`), so a future selector rename that breaks a screen fails this suite, not only the live one; verify the spec fails if a screen's selector constant is temporarily renamed (sanity-checked locally, reverted before commit).

## 5. CI wiring

- [ ] 5.1 Add an unconditional `go test ./login/chrome/...` (or a Ginkgo label filter selecting only the new mock-based specs, if the live specs can't otherwise be excluded from an unconditional step) to `.github/workflows/test.yml`, alongside the existing live-account step; verify by pushing and confirming the new step runs and passes on a PR without live secrets set, and that it does not affect the existing live step's behavior.

## 6. Verification

- [ ] 6.1 Run `golangci-lint run ./...` and `go test ./...` (with `GOWORK=off`) and confirm both are clean, including the new files.
- [ ] 6.2 Confirm `chrome_test.go`'s existing live specs are untouched and still gated the same way (diff review), so this change is additive only.
